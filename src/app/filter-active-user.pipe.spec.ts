import { FilterActiveUserPipe } from './filter-active-user.pipe';

describe('FilterActiveUserPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterActiveUserPipe();
    expect(pipe).toBeTruthy();
  });
});
