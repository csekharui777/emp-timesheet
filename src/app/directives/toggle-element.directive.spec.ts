import { ToggleElementDirective } from './toggle-element.directive';

describe('ToggleElementDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleElementDirective();
    expect(directive).toBeTruthy();
  });
});
