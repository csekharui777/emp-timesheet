import { SuccessBtnDirective } from './success-btn.directive';

describe('SuccessBtnDirective', () => {
  it('should create an instance', () => {
    const directive = new SuccessBtnDirective();
    expect(directive).toBeTruthy();
  });
});
