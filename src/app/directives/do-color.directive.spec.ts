import { DoColorDirective } from './do-color.directive';

describe('DoColorDirective', () => {
  it('should create an instance', () => {
    const directive = new DoColorDirective();
    expect(directive).toBeTruthy();
  });
});
